<?xml version="1.0" encoding="UTF-8"?>
<Configuration>
    <Appenders>
        <Console name="LogToConsole" target="SYSTEM_OUT">
            <PatternLayout pattern="%d{HH:mm:ss} [%t] %-5level %logger{36} - %msg%n"/>
        </Console>
        <!-- log 파일을 저장하는 위치와 정책 -->
        <RollingFile name="SaveFile">
            <FileName>${sys:user.home}/AppData/Roaming/MCServerBuilder/Logs/Log.log</FileName>
            <FilePattern>${sys:user.home}/AppData/Roaming/MCServerBuilder/Logs/%d{yyyy-MM-dd-HH}.log</FilePattern>
            <PatternLayout pattern="%d{yyyy-MMM-dd HH:mm:ss} [%t] %-5level %logger{36} - %msg%n"/>
            <Policies>
                <TimeBasedTriggeringPolicy interval="1" modulate="true" />
            </Policies>
            <DefaultRolloverStrategy max="7" fileIndex="max" >
                <Delete basePath = "{로그파일 저장 위치}" maxDepth = "1">
                    <!-- 3일이 경과한 로그파일은 자동 삭제 -->
                    <IfLastModified age = "3d"/>
                </Delete>
            </DefaultRolloverStrategy>
        </RollingFile>
    </Appenders>
    <Loggers>
        <!-- 기본(디폴트) loger 설정-->
        <Root level="INFO">
            <AppenderRef ref="LogToConsole"/>
            <AppenderRef ref="SaveFile"/>
        </Root>
    </Loggers>
</Configuration>